# JayTom.Dws 系统架构文档

## 概述
JayTom.Dws 采用清晰的分层架构设计，遵循关注点分离和依赖倒置原则，确保各层之间边界清晰，无侵入式耦合。

## 架构分层

### 1. 表现层 (Presentation Layer)
**项目**: `JayTom.Dws.Client`
- **职责**: WPF 客户端应用程序，用户界面和交互
- **依赖**: 应用层、基础设施层、设备层、插件接口
- **特点**: 
  - 使用 MVVM 模式
  - 通过依赖注入获取服务
  - 不直接访问数据层

### 2. 应用层 (Application Layer)
**项目**: `JayTom.Dws.Application`
- **职责**: 应用服务、DTO、业务用例编排
- **依赖**: 领域层、许可证模块
- **特点**:
  - 协调领域对象完成业务用例
  - 处理应用级别的业务逻辑
  - 提供 DTO 用于数据传输

### 3. 领域层 (Domain Layer)
**项目**: `JayTom.Dws.Domain`
- **职责**: 核心业务逻辑、领域模型、业务规则
- **依赖**: 数据层、接口层、插件层
- **特点**:
  - 包含领域实体和值对象
  - 定义业务规则和验证逻辑
  - 不依赖基础设施实现

### 4. 基础设施层 (Infrastructure Layer)
**项目**: `JayTom.Dws.Infrastructure`
- **职责**: 数据访问、外部服务集成、技术基础设施
- **依赖**: 数据层、领域层、插件层
- **特点**:
  - Entity Framework Core 实现
  - SignalR 实时通信
  - 缓存和弹性策略（Polly）
  - 数据库迁移和配置

### 5. 数据层 (Data Layer)
**项目**: `JayTom.Dws.Data`
- **职责**: 数据模型和实体定义
- **依赖**: 无（独立层）
- **特点**:
  - POCO 实体类
  - 数据传输对象
  - 无业务逻辑

### 6. 设备集成层 (Device Integration Layer)

#### 6.1 相机模块
**项目**: `JayTom.Dws.Camera`
- **职责**: 相机设备集成（海康威视、大华、USB 相机等）
- **依赖**: OCR 模块
- **支持设备**:
  - 工业相机（海康、大华、华睿）
  - 智能相机
  - 体积相机
  - 安防相机

#### 6.2 NVR 模块
**项目**: `JayTom.Dws.Nvr`
- **职责**: 网络视频录像机集成和视频录制
- **依赖**: 无

#### 6.3 OCR 模块
**项目**: `JayTom.Dws.Ocr`
- **职责**: 光学字符识别和图像处理
- **依赖**: 无
- **技术**:
  - ONNX Runtime
  - 快递单识别
  - 自定义 OCR 模型

#### 6.4 设备管理
**项目**: `JayTom.Dws.Device`
- **职责**: 通用设备管理和集成
- **依赖**: 无
- **支持设备**:
  - Percipio 设备
  - 华睿科技设备

### 7. 插件系统层 (Plugin System Layer)

#### 7.1 插件接口
**项目**: `JayTom.Dws.PluginInterface`
- **职责**: 插件接口定义和契约
- **依赖**: 无（独立层）
- **特点**:
  - 定义插件生命周期
  - 提供扩展点
  - 依赖注入集成

#### 7.2 插件实现
**项目**: `JayTom.Dws.Plugin`
- **职责**: 通用插件功能实现
- **依赖**: 无（独立层）
- **功能**:
  - 设备通信（串口、TCP、UDP）
  - 文件操作（FTP、本地文件）
  - 数据导出（Excel、PDF）
  - 语音播报

#### 7.3 Sunnen 插件
**项目**: `JayTom.Dws.SunnenPlugin`
- **职责**: Sunnen 特定业务功能插件
- **依赖**: 插件接口

**项目**: `JayTom.Dws.Sunnen`
- **职责**: Sunnen 业务逻辑
- **依赖**: 无

### 8. 横切关注点层 (Cross-Cutting Concerns Layer)

#### 8.1 横切关注点
**项目**: `JayTom.Dws.CrossCutting`
- **职责**: 日志、通信、共享功能
- **依赖**: 无（独立层）
- **功能**:
  - NLog 日志集成
  - SignalR 客户端
  - 事件总线

#### 8.2 工具类
**项目**: `JayTom.Dws.Utils`
- **职责**: 通用工具类和辅助方法
- **依赖**: 无（独立层）
- **功能**:
  - 图像处理工具
  - 数据转换工具
  - 通用帮助方法

#### 8.3 接口层
**项目**: `JayTom.Dws.Interface`
- **职责**: 服务接口和契约定义、外部 API 集成
- **依赖**: 插件层、工具层
- **功能**:
  - 服务契约
  - API 客户端
  - 云服务集成

#### 8.4 许可证管理
**项目**: `JayTom.Dws.License`
- **职责**: 许可证验证和管理
- **依赖**: 无（独立层）
- **功能**:
  - 硬件指纹识别
  - 许可证加密验证
  - 试用期管理

## 依赖关系图

```
┌─────────────────────────────────────────────────────────┐
│                    表现层                                │
│              JayTom.Dws.Client                          │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┼───────────┬──────────────┐
         │           │           │              │
         ▼           ▼           ▼              ▼
┌─────────────┐ ┌─────────┐ ┌─────────┐ ┌────────────┐
│  应用层      │ │基础设施层│ │ 设备层   │ │  插件层     │
│ Application │ │Infrastr-│ │ Camera  │ │ Plugin-    │
│             │ │ucture   │ │ Nvr     │ │ Interface  │
└──────┬──────┘ └────┬────┘ │ Device  │ └────────────┘
       │             │      │ Ocr     │
       │             │      └─────────┘
       ▼             ▼
┌─────────────┐ ┌─────────┐
│   领域层     │ │ 数据层   │
│   Domain    │ │  Data   │
└──────┬──────┘ └─────────┘
       │
       ▼
┌─────────────┐
│  接口层      │
│  Interface  │
└─────────────┘
       │
       ▼
┌─────────────┐
│  插件层      │
│   Plugin    │
└─────────────┘

横切关注点：
┌──────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ CrossCutting │ │  Utils   │ │ License  │ │Interface │
└──────────────┘ └──────────┘ └──────────┘ └──────────┘
```

## 架构原则

### 1. 依赖倒置原则 (Dependency Inversion)
- 高层模块不依赖低层模块，都依赖于抽象
- 接口定义在高层，实现在低层
- 使用依赖注入容器管理依赖

### 2. 关注点分离 (Separation of Concerns)
- 每个项目有明确的职责
- 避免跨层直接访问
- 通过接口进行层间通信

### 3. 零边界入侵 (Zero Boundary Intrusion)
- 数据层完全独立，无外部依赖
- 工具层和横切关注点层独立
- 插件系统通过接口隔离
- 领域层不依赖具体实现

### 4. 清晰命名 (Clear Naming)
- 项目名称反映其职责
- 使用统一的命名空间前缀 `JayTom.Dws`
- 分层清晰可识别

## 包管理策略

### 集中式包版本管理
项目使用 `Directory.Packages.props` 进行集中式包版本管理：

**优势**：
- 统一版本控制
- 避免版本冲突
- 简化依赖管理

**使用方法**：
```xml
<!-- Directory.Packages.props 中定义版本 -->
<PackageVersion Include="Newtonsoft.Json" Version="13.0.3" />

<!-- .csproj 中引用（不指定版本） -->
<PackageReference Include="Newtonsoft.Json" />
```

**关键包**：
- **依赖注入**: DryIoc, Microsoft.Extensions.DependencyInjection
- **UI**: MaterialDesignThemes, Prism
- **数据访问**: Entity Framework Core 5.0
- **通信**: SignalR, TouchSocket
- **序列化**: Newtonsoft.Json
- **日志**: NLog
- **弹性**: Polly
- **硬件**: System.Management, System.Drawing.Common
- **视频**: Accord.Video.FFMPEG, FFmpeg.AutoGen
- **机器学习**: Microsoft.ML.OnnxRuntime.Gpu

## 技术栈

### 开发框架
- **.NET 7.0**: 主要开发框架
- **.NET 6.0**: 部分工具项目
- **WPF**: 桌面应用程序框架

### 数据访问
- **Entity Framework Core 5.0**: ORM
- **SQLite**: 嵌入式数据库
- **SQL Server**: 企业级数据库

### UI 框架
- **MaterialDesignThemes**: Material Design 风格
- **Prism**: MVVM 框架
- **DryIoc**: IoC 容器

### 设备 SDK
- 海康威视 SDK
- 大华 SDK
- 华睿科技 SDK
- FFmpeg (视频处理)
- ONNX Runtime (AI 推理)

## 构建和部署

### 构建解决方案
```bash
# 完整解决方案
dotnet build JayTom.Dws.sln

# 客户端精简解决方案
dotnet build JayTom.Dws.Client.sln
```

### 运行应用程序
```bash
dotnet run --project JayTom.Dws.Client/JayTom.Dws.Client.csproj
```

## 扩展性

### 添加新插件
1. 实现 `JayTom.Dws.PluginInterface` 中定义的接口
2. 注册到依赖注入容器
3. 通过插件系统动态加载

### 添加新设备支持
1. 在相应设备项目中添加设备驱动
2. 实现设备接口
3. 注册设备工厂

### 添加新业务模块
1. 在应用层添加应用服务
2. 在领域层添加领域逻辑
3. 在基础设施层添加持久化实现
4. 在表现层添加 UI

## 质量保证

### 架构验证
- ✅ 依赖方向正确（从外向内）
- ✅ 无循环依赖
- ✅ 核心层无基础设施依赖
- ✅ 接口层与实现层分离

### 编码规范
- 遵循 C# 编码规范
- 使用异步编程模式
- 实现适当的异常处理
- 添加日志记录

## 安全性

### 许可证管理
- 硬件指纹验证
- 许可证文件加密
- 试用期控制

### 数据安全
- 敏感数据加密存储
- 安全的网络通信
- 访问控制

## 性能优化

### 缓存策略
- 内存缓存常用数据
- Polly 缓存策略

### 异步处理
- 所有 I/O 操作异步化
- 使用 async/await 模式

### 资源管理
- 正确释放设备资源
- 使用 using 语句管理生命周期
- 对象池优化

## 维护和演进

### 版本历史
- **2025-10-23**: 
  - 修复集中式包管理违规问题
  - 完善架构文档
  - 优化依赖关系

### 未来计划
- 迁移到事件驱动架构
- 升级到 .NET 8.0 LTS
- 增强可测试性
- 改进监控和可观测性

## 总结

JayTom.Dws 采用清晰的分层架构，确保：
- **可维护性**: 清晰的分层和职责划分
- **可扩展性**: 插件系统支持动态扩展
- **可测试性**: 依赖倒置便于单元测试
- **灵活性**: 松耦合设计支持快速变化
- **零侵入**: 核心层独立，边界清晰

架构设计遵循业界最佳实践，为项目的长期发展奠定了坚实基础。
