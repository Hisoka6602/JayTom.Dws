<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:models="clr-namespace:JayTom.Dws.Client.Models">
    <DataTemplate x:Key="MenuTemplate" DataType="{x:Type models:MenuItemInfoModel}">
        <!--<Button Height="50">
            <Button.Style>
                <Style BasedOn="{StaticResource MaterialDesignFlatAccentBgButton}" TargetType="Button">
                    <Setter Property="Margin" Value="10" />
                    <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="15" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TransformGroup>
                                <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="1" ScaleY="1" />
                                <TranslateTransform X="0" Y="0" />
                            </TransformGroup>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
            <Grid
                Height="60"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="20" />
                </Grid.RowDefinitions>
                ~1~  图标  @1@
                <Border
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding Icon}" />
                    </Border.Background>
                </Border>
                ~1~  字体图标  @1@
                <TextBlock
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="{Binding IconFont.IconFont}"
                    FontSize="{Binding IconFont.IconSize}"
                    Foreground="{Binding IconFont.IconColor}"
                    Text="{Binding IconFont.IconCode}" />
                ~1~<TextBlock
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Fonts/#iconfont"
                    FontSize="40"
                    Foreground="White"
                    Text="&#xe648;" />@1@
                ~1~  标题  @1@
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Fonts/#更纱黑体 Mono SC Nerd"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="{Binding Title}" />
                ~1~  说明  @1@
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Fonts/#FiraCode-Bold"
                    FontSize="10"
                    Foreground="White"
                    Text="{Binding Description}" />
            </Grid>
        </Button>-->
        <Button
            x:Name="MenuButton"
            Padding="10,0"
            Command="{Binding ClickCommand}"
            CommandParameter="{Binding}"
            ToolTip="{Binding Description}">
            <Button.Style>
                <Style BasedOn="{StaticResource MaterialDesignFlatAccentBgButton}" TargetType="Button">
                    <Setter Property="Margin" Value="10" />
                    <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="15" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TransformGroup>
                                <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="1" ScaleY="1" />
                                <TranslateTransform X="0" Y="0" />
                            </TransformGroup>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>

            <StackPanel
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <!--  图标  -->
                <Grid>
                    <Border
                        Grid.Column="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Icon}" />
                        </Border.Background>
                    </Border>
                    <TextBlock
                        x:Name="IconBlock"
                        Margin="5,0"
                        VerticalAlignment="Center"
                        FontFamily="{Binding IconFont.IconFont}"
                        FontSize="{Binding IconFont.IconSize}"
                        FontWeight="Light"
                        Foreground="DarkGray"
                        Text="{Binding IconFont.IconCode}" />
                </Grid>

                <!--  文字  -->
                <TextBlock
                    x:Name="DescriptionBlock"
                    Margin="2,2,0,0"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Fonts/#Sarasa Mono SC Nerd"
                    FontSize="16"
                    Foreground="DarkGray"
                    Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Button>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter TargetName="IconBlock" Property="Foreground" Value="White" />
                <Setter TargetName="DescriptionBlock" Property="Foreground" Value="White" />
                <Setter TargetName="MenuButton" Property="Background" Value="#4A9DF8" />
            </DataTrigger>
            <!--  多条件  -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ElementName=MenuButton, Path=IsMouseOver}" Value="True" />
                    <Condition Binding="{Binding IsSelected}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="IconBlock" Property="Foreground" Value="White" />
                <Setter TargetName="DescriptionBlock" Property="Foreground" Value="White" />
                <Setter TargetName="MenuButton" Property="Background" Value="DodgerBlue" />
                <MultiDataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="MenuButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                To="1.1"
                                Duration="0:0:0.2" />
                            <DoubleAnimation
                                Storyboard.TargetName="MenuButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                To="1.1"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </MultiDataTrigger.EnterActions>
                <MultiDataTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="MenuButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                Duration="0:0:0.2" />
                            <DoubleAnimation
                                Storyboard.TargetName="MenuButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </MultiDataTrigger.ExitActions>
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>