<Page
    x:Class="JayTom.Dws.Client.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="http://prismlibrary.com/"
    xmlns:dialog="clr-namespace:JayTom.Dws.Client.Views.Dialog"
    Name="SettingPage"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    Focusable="False"
    FontFamily="{StaticResource CustomFontFamily}"
    mc:Ignorable="d">
    <Page.Resources>
        <materialDesign:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded" SourceName="SettingPage">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding ElementName=RegionFrame}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <materialDesign:DialogHost
        Background="Transparent"
        DialogBackground="Transparent"
        DialogMargin="5"
        FocusVisualStyle="{x:Null}"
        Focusable="False"
        Identifier="SettingDialog"
        Style="{StaticResource MaterialDesignEmbeddedDialogHost}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="210" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!--  菜单列表  -->
            <Grid Grid.Column="0" Width="210">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ListBox
                    Grid.Row="0"
                    MinHeight="100"
                    MaxHeight="{Binding ListBoxMaxHeight, UpdateSourceTrigger=PropertyChanged}"
                    ItemTemplate="{StaticResource MenuTemplate}"
                    ItemsSource="{Binding MenuItems, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource CustomListBoxStyle}" />
                <Grid
                    Grid.Row="1"
                    MinWidth="1000"
                    Background="#2E2E2E" />
            </Grid>

            <!--  区域内容  -->
            <!--<ScrollViewer
            Grid.Column="1"
            Focusable="False"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto">
        </ScrollViewer>-->
            <Frame
                Name="RegionFrame"
                Grid.Column="1"
                mvvm:RegionManager.RegionName="ContentRegion"
                Focusable="False"
                NavigationUIVisibility="Hidden" />
            <TextBlock
                Grid.Column="1"
                Margin="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                FontSize="13"
                FontWeight="Bold"
                Foreground="White"
                Opacity="0.1"
                Visibility="Visible">
                Hisoka
            </TextBlock>

            <!--  常驻加载遮罩不依赖 DialogHost 的注册时序，避免首次导航出现宿主未加载异常。  -->
            <Grid
                Grid.ColumnSpan="2"
                Panel.ZIndex="1000"
                Background="#99000000"
                IsHitTestVisible="{Binding IsLoading}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="Opacity" Value="0" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Opacity" Value="1" />
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                From="0"
                                                To="1"
                                                Duration="0:0:0.18">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <dialog:LoadingDialog
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    DataContext="{Binding LoadingDialog}" />
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Page>
