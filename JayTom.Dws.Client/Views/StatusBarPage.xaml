<Page
    x:Class="JayTom.Dws.Client.Views.StatusBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:JayTom.Dws.Client.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:JayTom.Dws.Client.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="http://prismlibrary.com/"
    Title="StatusBarPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    FontFamily="{StaticResource CustomFontFamily}"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:ListBackgroundVisibleConverter x:Key="ListBackgroundVisibleConverter" />
        <converters:ExceptionFlagColorConverter x:Key="ExceptionFlagColorConverter" />
        <converters:NullStatusFlagColorConverter x:Key="NullStatusFlagColorConverter" />
        <converters:CameraStatusSummaryColorConverter x:Key="CameraStatusSummaryColorConverter" />
        <converters:SerialPortStatusSummaryColorConverter x:Key="SerialPortStatusSummaryColorConverter" />
        <converters:MemoryRemainingColorConverter x:Key="MemoryRemainingColorConverter" />
        <converters:ComputerColorConverter x:Key="ComputerColorConverter" />
    </Page.Resources>
    <Grid
        Margin="10,5"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Center">
        <StackPanel
            MinWidth="1700"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <!--  状态  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        Foreground="DimGray"
                        Text="&#xe6f9;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="异常" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding ExceptionItems.Count, Converter={StaticResource ExceptionFlagColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid ClipToBounds="True">
                            <ContentPresenter />
                            <Popup
                                IsOpen="{TemplateBinding IsMouseOver}"
                                Placement="Top"
                                StaysOpen="False"
                                Style="{StaticResource LazyUpdatePopupStyle}">
                                <!--  在这里定义 Popup 的内容  -->
                                <Grid
                                    Width="250"
                                    Height="300"
                                    Background="#303030"
                                    ClipToBounds="True">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="40" />
                                    </Grid.RowDefinitions>
                                    <!--  异常信息列表  -->
                                    <ListBox
                                        x:Name="ExceptionListBox"
                                        Grid.Row="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        ItemTemplate="{StaticResource ExceptionItemTemplate}"
                                        ItemsSource="{Binding ExceptionItems, UpdateSourceTrigger=PropertyChanged, IsAsync=True}"
                                        Style="{StaticResource CustomListBoxStyle}" />
                                    <!--  清除按钮  -->
                                    <Button
                                        Grid.Row="1"
                                        Width="100"
                                        Height="30"
                                        Margin="5"
                                        VerticalAlignment="Bottom"
                                        materialDesign:ButtonAssist.CornerRadius="15"
                                        Background="RoyalBlue"
                                        BorderBrush="RoyalBlue"
                                        BorderThickness="0"
                                        Command="{Binding ClearExceptionCommand}"
                                        Style="{StaticResource MaterialDesignRaisedDarkButton}">
                                        <TextBlock
                                            FontFamily="{StaticResource CustomFontFamily}"
                                            FontWeight="Bold"
                                            Foreground="White"
                                            Text="清空异常" />
                                    </Button>
                                    <!--  空数据底图  -->
                                    <Border
                                        Grid.Row="0"
                                        Grid.RowSpan="2"
                                        Background="#303030"
                                        Visibility="{Binding Items.Count, ElementName=ExceptionListBox, Converter={StaticResource ListBackgroundVisibleConverter}}">
                                        <StackPanel
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Orientation="Vertical">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="50"
                                                Text="&#xe63d;" />
                                            <TextBlock
                                                Margin="0,10"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource CustomFontFamily}"
                                                FontSize="16"
                                                Text="没有数据" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <!--  相机  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        Foreground="DimGray"
                        Text="&#xe63a;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="相机" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding CameraItems, Converter={StaticResource CameraStatusSummaryColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid ClipToBounds="True">
                            <ContentPresenter />
                            <Popup
                                IsOpen="{TemplateBinding IsMouseOver}"
                                Placement="Top"
                                StaysOpen="False"
                                Style="{StaticResource LazyUpdatePopupStyle}">
                                <!--  在这里定义 Popup 的内容  -->
                                <Grid
                                    Width="250"
                                    Height="300"
                                    Background="#303030"
                                    ClipToBounds="True">

                                    <!--  相机信息列表  -->
                                    <ListBox
                                        Grid.Row="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        ItemTemplate="{StaticResource CameraStatusBarTemplate}"
                                        ItemsSource="{Binding CameraItems, UpdateSourceTrigger=PropertyChanged, IsAsync=True}"
                                        Style="{StaticResource CustomListBoxStyle}" />
                                    <!--  空数据底图  -->
                                    <Border
                                        Grid.Row="0"
                                        Background="#303030"
                                        Visibility="Hidden">
                                        <StackPanel
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Orientation="Vertical">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="50"
                                                Text="&#xe63d;" />
                                            <TextBlock
                                                Margin="0,10"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource CustomFontFamily}"
                                                FontSize="16"
                                                Text="没有数据" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <!--  串口信息  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        Foreground="DimGray"
                        Text="&#xe62c;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="串口" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding SerialPortItems, Converter={StaticResource SerialPortStatusSummaryColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid ClipToBounds="True">
                            <ContentPresenter />
                            <Popup
                                IsOpen="{TemplateBinding IsMouseOver}"
                                Placement="Top"
                                StaysOpen="False"
                                Style="{StaticResource LazyUpdatePopupStyle}">
                                <!--  在这里定义 Popup 的内容  -->
                                <Grid
                                    Width="250"
                                    Height="300"
                                    Background="#303030"
                                    ClipToBounds="True">

                                    <!--  相机信息列表  -->
                                    <ListBox
                                        Grid.Row="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        ItemTemplate="{StaticResource SerialPortTemplate}"
                                        ItemsSource="{Binding SerialPortItems, UpdateSourceTrigger=PropertyChanged, IsAsync=True}"
                                        Style="{StaticResource CustomListBoxStyle}" />
                                    <!--  空数据底图  -->
                                    <Border
                                        Grid.Row="0"
                                        Background="#303030"
                                        Visibility="Hidden">
                                        <StackPanel
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Orientation="Vertical">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="50"
                                                Text="&#xe63d;" />
                                            <TextBlock
                                                Margin="0,10"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource CustomFontFamily}"
                                                FontSize="16"
                                                Text="没有数据" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <!--  电脑信息  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                DataContext="{Binding}"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}"
                Template="{StaticResource ComputerInfoTemplate}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        Foreground="DimGray"
                        Text="&#xe67b;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="监控" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding ComputerInfo, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource ComputerColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
            </Button>
            <!--  Cpu  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Cursor=""
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        Foreground="DimGray"
                        Text="&#xe84d;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray">
                        <Run Text="Cpu:" /><Run Text="{Binding ComputerInfo.CpuInfo.UsagePercentage, StringFormat={}{0:F1}}" /><Run Text="%" />
                    </TextBlock>
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding ComputerInfo.CpuInfo.UsagePercentage, Converter={StaticResource CpuUsageColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
            </Button>
            <!--  温度  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="&#xe660;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray">
                        <Run Text="温度:" /><Run Text="{Binding ComputerInfo.CpuInfo.CpuTemperature, StringFormat={}{0:F1}}" /><Run Text="°" />
                    </TextBlock>
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding ComputerInfo.CpuInfo.CpuTemperature, Converter={StaticResource CpuTemperatureColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
            </Button>
            <!--  内存  -->
            <Button
                Margin="2,0"
                Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Focusable="False"
                Style="{StaticResource MaterialDesignFlatButton}">
                <StackPanel Orientation="Horizontal">

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="DimGray"
                        Text="&#xe60f;" />
                    <TextBlock
                        Margin="5,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CustomFontFamily}"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="DimGray">
                        <Run Text="内存:" /><Run Text="{Binding ComputerInfo.MemoryInfo.UsedPercentage, StringFormat={}{0:F1}}" /><Run Text="%" />
                    </TextBlock>
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFontFamily}"
                        FontSize="30"
                        Foreground="{Binding ComputerInfo.MemoryInfo.UsedPercentage, Converter={StaticResource MemoryUsedPercentageColorConverter}}"
                        Text="&#xe7a6;" />
                </StackPanel>
            </Button>
        </StackPanel>
        <!--  运行时间  -->
        <StackPanel
            Margin="10,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="{StaticResource IconFontFamily}"
                FontSize="20"
                Foreground="DimGray"
                Text="&#xe635;" />
            <TextBlock
                Margin="5,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="{StaticResource CustomFontFamily}"
                FontSize="14"
                FontWeight="Bold"
                Foreground="DimGray"
                Text="0天-00:00:00" />
        </StackPanel>
    </Grid>
</Page>