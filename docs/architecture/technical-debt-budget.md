# 技术债预算门禁

`eng/CodeQualityBaseline.json` 是存量技术债的逐文件上限，不是允许新增违规的豁免表。代码质量守卫在本地 Client 构建和 CI Release 门禁中执行；任一分类、任一文件的计数增长都会使构建失败。

迭代规则：新增债务预算恒为零；修复后允许计数下降；不得通过抬高基线合并变更。确需调整检测规则时，必须在架构决策记录中说明误报样例、替代验收和回滚方式。
