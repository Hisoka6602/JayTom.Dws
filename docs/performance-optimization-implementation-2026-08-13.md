# 热路径性能与错分防护：100 项落地矩阵

日期：2026-08-13

状态说明：`本轮实现`表示本次直接修改；`已复核`表示代码原本已具备并在本轮纳入整体校验；`回归保护`表示通过测试或质量门禁固化。

## 一、包裹身份、赋值与生命周期

1. **本轮实现**：为活动包裹维护稳定创建顺序索引，避免每次匹配重新排序（`PackageInfoManager`）。
2. **本轮实现**：单独维护未赋值包裹索引，条码匹配不扫描已赋值会话（`PackageInfoManager`）。
3. **本轮实现**：建立下位机包裹序号到创建时间的 O(1) 索引（`PackageInfoManager`）。
4. **本轮实现**：重复下位机包裹序号拒绝进入活动会话，避免两包共享身份（`PackageInfoManager`）。
5. **本轮实现**：条码绑定在包裹锁内原子完成，禁止并发条码覆盖（`TryBindBarcode`）。
6. **本轮实现**：条码赋值最小/最大间隔完全读取客户端配置，不硬编码生产边界（`CreatePackageSettingsDto`、`YunShanPackageBackgroundService`）。
7. **本轮实现**：配置保存前校验负数、零窗口和上下限倒置（`CreatePackageSettingsDto.TryValidate`）。
8. **本轮实现**：按设备观测时间匹配包裹，避免工作线程调度延迟改变机械对应关系（`TryBindBarcode`）。
9. **本轮实现**：同时校验程序实际处理时间，排队越过空包裹期限后禁止赋值（`TryBindBarcode`）。
10. **本轮实现**：400ms 空包裹删除边界采用 `>=`，删除优先于条码赋值（`TryBindBarcode`）。
11. **本轮实现**：过期队首删除后，同一条码禁止顺延到后续包裹（`TryBindBarcode`）。
12. **本轮实现**：倒序模式跳过未达到最小间隔的新包裹，选择最新成熟候选（`SelectBarcodeCandidateKey`）。
13. **本轮实现**：倒序模式仍优先处理过期队首，不能绕过400ms删除规则（`SelectBarcodeCandidateKey`）。
14. **本轮实现**：早于包裹创建的条码直接拒绝，不误删未来包裹（`TryBindBarcode`）。
15. **本轮实现**：已赋值活动包裹不再阻止下一包裹创建（`HasUnassignedPackage`）。
16. **本轮实现**：包裹添加失败时回滚顺序、未赋值和下位机序号索引（`TryAddPackage`）。
17. **本轮实现**：计时器启动失败时回滚半初始化会话（`TryAddPackage`）。
18. **本轮实现**：包裹完成按已知创建时间直接定位，不再扫描全部会话（`CompletedPackage(DateTime)`）。
19. **本轮实现**：包裹完成标记不可逆，重复完成事件不会重复发布（`PackageInfo.MarkCompleted`）。
20. **本轮实现**：条码引用采用原子发布，读线程不会观察到撕裂状态（`PackageInfo.BarCodeInfo`）。
21. **本轮实现**：空包裹到期回调执行前重新校验仍为空，避免删除刚完成赋值的包裹（`PackageInfoManager`）。
22. **本轮实现**：生命周期从机械创建时间扣除注册延迟，程序卡顿不会给空包裹重新续期（`PackageInfo.StartTimers`）。
23. **本轮实现**：完成包裹在物理格口指令全部发送前保持活动身份（`IsWaitingForSortingInstruction`）。
24. **本轮实现**：物理发送全部成功后才标记格口生命周期完成（`OnSendSucceeded`）。
25. **本轮实现**：无分拣模式明确释放格口生命周期等待（`MarkSortingInstructionNotRequired`）。
26. **本轮实现**：图片已交接不再等同于“可以删除包裹”（`YunShanPackageBackgroundService`）。
27. **本轮实现**：完成包裹清理由专用队列跟踪，不再每100ms扫描全部活动会话。
28. **本轮实现**：待清理队列使用原子 O(1) 计数，不调用 `ConcurrentQueue.Count`。
29. **本轮实现**：单轮清理设4096项上限，异常积压不长期占用维护线程。
30. **本轮实现**：启动期分拣未就绪时保守保留包裹，并按到达顺序暂存完成/API/OCR事件。

## 二、截止时间、队列与线程隔离

31. **本轮实现**：用单调时钟调度包裹生命周期，系统时间调整不改变期限（`PackageTimerScheduler`）。
32. **本轮实现**：截止判断线程与业务回调线程分离，慢回调不推迟后续到期判断。
33. **本轮实现**：空包裹删除进入最高优先级关键队列，越过密集普通事件。
34. **本轮实现**：创建、读码、空读、回调、赋值由单消费者严格有序处理。
35. **本轮实现**：关键生产者仅执行无等待入队，不执行日志、UI、数据库或网络 I/O。
36. **本轮实现**：关键队列使用专属高优先级长驻线程，不依赖普通线程池调度。
37. **本轮实现**：密集入队合并内核唤醒，一个活跃周期只发一次信号。
38. **本轮实现**：高优先级和普通队列分别保持自身 FIFO 顺序。
39. **本轮实现**：队列记录最大排队耗时和单项执行耗时，单位为微秒。
40. **本轮实现**：队列待处理数使用原子计数，诊断不枚举容器。
41. **本轮实现**：停机时先禁止新生产者，再等待越过边界的生产者完成入队。
42. **本轮实现**：消费者仅在生产者归零且队列为空后退出，修复停机尾项丢失竞态。
43. **本轮实现**：单项处理异常隔离，不能终止关键工作线程。
44. **本轮实现**：错误回调自身异常也被隔离，不能二次击穿消费者。
45. **本轮实现**：完成事件、图片交接、普通通知拆为不同队列，互不反压。
46. **本轮实现**：API格口响应使用独立高优先级发布线程。
47. **本轮实现**：API日志/UI通知使用独立普通优先级发布线程。
48. **本轮实现**：分拣计算使用独立高优先级有序线程。
49. **本轮实现**：每个物理连接使用独立发送队列，同连接严格有序、不同连接互不阻塞。
50. **本轮实现**：分拣延迟发送使用单调截止调度器，不用占用线程睡眠。

## 三、API、格口计算与物理通信

51. **已复核**：包裹API最大滚动并发为600（`ApiQueryMaxConcurrency`）。
52. **已复核**：HTTP每服务器连接池上限为600，与业务并发一致（`MaxConnectionsPerServer`）。
53. **本轮实现**：每个活动任务只承载一个包裹，请求完成后立即补位。
54. **本轮实现**：活动请求数使用原子计数，不读取并发字典 `Count`。
55. **本轮实现**：HTTP流程直接启动，不额外 `Task.Run` 进入线程池排队。
56. **本轮实现**：每个并发请求解析独立上传器，避免共享可变提供商状态。
57. **本轮实现**：API失败使用指数退避，防止600路同时形成重试风暴。
58. **本轮实现**：API工作队列无损，临时网络故障不静默丢包裹请求。
59. **本轮实现**：API响应携带创建时间、毫秒时间戳、下位机序号、条码和扫码时间完整身份。
60. **本轮实现**：消费API响应前逐字段核对当前活动包裹身份。
61. **本轮实现**：同一包裹API响应通过原子集合只允许消费一次。
62. **本轮实现**：包裹移除后清理API一次性消费状态，集合保持有界。
63. **本轮实现**：API规则在配置加载时预解析、排序，响应热路径不重复反序列化配置。
64. **本轮实现**：单个API响应只解析一次JSON文档，全部规则共享。
65. **本轮实现**：API分拣配置主键建立O(1)格口索引。
66. **本轮实现**：格口定义、备用格口和异常格口一次性构建原子快照。
67. **本轮实现**：格口到指令绑定建立O(1)索引。
68. **本轮实现**：指令按绑定预分组，发送前不再 `Where().ToList()`。
69. **本轮实现**：配置更新异步重建索引，热路径只读快照。
70. **本轮实现**：配置快照一次性原子发布，杜绝新旧数组混读。
71. **本轮实现**：锁格切换备用格口后统一使用最终生效格口编号查绑定、附件和连接。
72. **本轮实现**：发送前再次核对活动包裹身份，延迟期间包裹变化则禁止发送。
73. **本轮实现**：每条重试发送前再次核对身份，避免旧重试落到新包裹。
74. **本轮实现**：附件目标格口与实际发送格口不一致时立即拒绝。
75. **本轮实现**：格口到连接建立O(1)原子映射。
76. **本轮实现**：默认连接按配置主键确定性排序，不依赖字典迭代顺序。
77. **本轮实现**：无有效连接时立即失败，不再把工作排到空连接键。
78. **本轮实现**：回包按连接隔离通道，多个下位机不会互相消费应答。
79. **本轮实现**：同一接收报文只进入一次应答通道，消除重复回包污染下一指令。
80. **本轮实现**：原始回包先进入匹配通道，再异步做协议、日志和UI处理。
81. **本轮实现**：回包协议解析严格按到达顺序执行。
82. **本轮实现**：指令发送记录异步发布，落库/界面不阻塞物理发送。
83. **本轮实现**：分拣计算队列按分钟报告排队、执行和积压水位。
84. **本轮实现**：物理连接发送队列按分钟报告排队、执行和积压水位。

## 四、相机SDK、图片、持久化与日志

85. **本轮实现**：相机SDK回调只复制仍有消费者需要的帧，无条码/预览订阅时立即返回。
86. **本轮实现**：USB相机在SDK回调内先判断跳帧，生产模式不复制随后丢弃的位图。
87. **本轮实现**：只有实时预览开启时才保留USB跳过读码的帧。
88. **本轮实现**：USB待处理区只保留最新一帧，旧帧立即释放，避免读码积压持续增长。
89. **本轮实现**：工业相机大图使用 `ArrayPool` 缓冲，降低大对象堆分配与GC停顿。
90. **本轮实现**：相机帧由专属高优先级长驻线程有序消费，不占普通线程池配额。
91. **本轮实现**：相机线程按分钟限频报告超过50ms的排队或处理水位。
92. **本轮实现**：多相机融合使用首帧设备时间匹配包裹，不用较慢相机的完成时间。
93. **本轮实现**：多相机关联窗口受客户端创建间隔约束，防止合并相邻机械包裹。
94. **本轮实现**：同组相机条码冲突时整组拒绝，禁止任取一个造成错分。
95. **本轮实现**：未到齐相机组到期后拒绝赋值并释放全部图像。
96. **本轮实现**：完成包裹图片立即进入独立交接队列，移除原100ms每次一张的10张/秒上限。
97. **本轮实现**：相机序列号到名称使用O(1)字典快照，存图交接不遍历相机列表。
98. **本轮实现**：FTP上传移到低优先级专属队列，本地存图不等待网络。
99. **本轮实现**：SQLite启用WAL、NORMAL同步、30秒忙等待、内存临时表、32MiB缓存、256MiB映射及关联索引；日志异步写且不独占文件，因此运行中可复制日志。
100. **回归保护**：质量守卫、关键并发/边界测试、完整构建和全量测试共同阻止热路径I/O、阻塞等待、额外分配、硬编码窗口、错位回归与停机丢项。

## 现场验收关注项

- 观察 `热路径性能水位(us)`、`分拣热路径性能水位(us)`、`格口发送性能水位(us)`、`相机帧处理性能水位` 四类限频日志。
- 若再次出现400ms以上延迟，可据此直接区分：包裹关键队列、API服务端、分拣计算、物理连接发送或相机SDK处理。
- 代码保证拒绝不确定关联，而不是在缺少身份依据时“猜一个包裹”；现场仍需用真实线速做长时间压力验收。
